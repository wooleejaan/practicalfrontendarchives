import Accordion from "@/patterns/compound";

export default function CompoundComponentPage() {
  return (
    <Accordion>
      <Accordion.Item active={true} onClick={() => {}} />
    </Accordion>
  );
}
